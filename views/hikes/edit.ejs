<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
    <style>
        /* Full-screen modal overlay */
        .edit {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background: rgba(0, 0, 0, 0.6);
          display: flex;
          align-items: center;
          justify-content: center;
          z-index: 1000;
        }
        
        /* Modal box */
        .overlay-form {
          position: relative;
          background: var(--menu-bg);
          color: var(--text);
          padding: 30px;
          border-radius: 8px;
          box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
          max-width: 500px;
          width: 90%;
          max-height: 90vh;
          overflow-y: auto;
        }
        
        /* Close button */
        .close-btn {
          position: absolute;
          top: 12px;
          right: 12px;
          background: none;
          border: none;
          font-size: 1.8rem;
          cursor: pointer;
          color: var(--text);
          padding: 0;
          width: 30px;
          height: 30px;
          display: flex;
          align-items: center;
          justify-content: center;
          text-decoration: none;
        }
        
        .close-btn:hover {
          opacity: 0.7;
        }
        
        .overlay-form form fieldset {
          border: none;
          padding: 0;
        }
        
        .overlay-form legend {
          font-size: 1.3rem;
          font-weight: bold;
          margin-bottom: 16px;
          color: var(--text);
        }
        
        .overlay-form label {
          display: block;
          margin: 12px 0;
          font-weight: 500;
          color: var(--text);
        }
        
        .overlay-form input {
          width: 100%;
          padding: 8px;
          margin-top: 4px;
          border: 1px solid #ccc;
          border-radius: 4px;
          background: var(--bg);
          color: var(--text);
          box-sizing: border-box;
        }
        
        .overlay-form input[type="checkbox"] {
          width: auto;
          margin-right: 8px;
        }
        
        .overlay-form button {
          background: var(--button-bg);
          color: var(--button-text);
          border: none;
          padding: 10px 20px;
          margin-top: 16px;
          border-radius: 4px;
          cursor: pointer;
          font-size: 1rem;
          width: 100%;
        }
        
        .overlay-form button:hover {
          background: var(--button-hover-bg);
        }
        
        @media (max-width: 600px) {
          .overlay-form {
            width: 95%;
            padding: 20px;
          }
        }
    </style>
<body>
    <%- include("../partials/header.ejs") %>

    <main>
        <div class="edit">
            <div class="overlay-form">
            <a href="/hikes/<%= hike._id %>" class="close-btn">✕</a>
            <form action="/hikes/<%= hike._id %>?_method=PUT" method="POST">
                <fieldset>
                    <legend>Edit <%= hike.hikeName %></legend><br>
                    <label>Hike To:<input
                        type="text"
                        name="hikeName"
                        placeholder="hike to what or where"
                        value="<%= hike.hikeName %>"/></label><br>
                    <label>Rank:<input
                        type="number"
                        name="rank"
                        placeholder="rank order"
                        value="<%= hike.rank %>"/></label><br>
                    <label>Elevation:<input
                        type="number"
                        name="elevation"
                        placeholder="high in feet"
                        value="<%= hike.elevation %>"/></label><br>
                    <label>Hike Complete:<input
                        type="checkbox"
                        name="hikeComplete"
                        <%= hike.hikeComplete ? "checked" : "" %>/></label><br>
                    <label>Date Complete:<input
                        type="date"
                        name="dateComplete"
                        placeholder="date"
                        value="<%= hike.dateComplete ? hike.dateComplete : '1900-01-01' %>"/></label><br>
                    <label>Image:<input
                        type="text"
                        name="img"
                        placeholder="image"
                        value="<%= hike.img %>"/></label>
                </fieldset>
                <button>Update Adventure</button>
            </form>
            </div>
        </div>
    </main>
</body>
</html>